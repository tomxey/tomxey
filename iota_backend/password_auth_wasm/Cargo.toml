[package]
name = "password_auth_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
password_auth_core = { path = "../password_auth_core" }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
